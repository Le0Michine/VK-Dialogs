import { VideoControlDirective } from './video-control.directive';

describe('VideoControlDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoControlDirective();
    expect(directive).toBeTruthy();
  });
});
